pipeline {
    agent any

    tools {
        maven 'Maven'
        jdk 'jdk-17'
    }

    stages {
        stage('Build') {
            steps {
                
                sh ' cd oorder && mvn clean install -DskipTests'
            }
        }
        
    }

    post {
        always {
            junit 'oorder/target/surefire-reports/*.xml'
        }
    }
}
